[project]
name = "field-guide"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = [
    "defusedxml>=0.7.1",
    "openpyxl>=3.1.5",
    "pymupdf>=1.28.0",
    "pytest>=9.1.1",
    "python-docx>=1.2.0",
    "pyyaml>=6.0.3",
]

[tool.pytest.ini_options]
pythonpath = ["."]
